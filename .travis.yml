language: python
python:
  - "3.6"

services:
  - docker

before_script: pip install docker-compose

script:
 - docker-compose run app sh -c "coverage run --source='.' manage.py wait_for_db && python manage.py test && flake8"

 after_success:
  - docker-compose run app sh -c "coverage report"